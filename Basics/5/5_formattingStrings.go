package main

import "fmt"

func main() {
	name := "Uday"
	age := 18
	color, number := "Blue", 4

	fmt.Printf("%s is %d years old\n", name, age)
	fmt.Printf("%s's fav color is %s and fav number is %d", name, color, number)

}